// selected restaurant page
import React, { useEffect, useState } from "react";
import { View } from "react-native";

export default function SelectedScreen({navigation}) {
    return (
        <View>
        </View>
    )
}